services:
  backend:
    build:
      context: ..
      dockerfile: docker/Dockerfile.backend  # if you named it differently, otherwise just Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ../backend:/app
      - /app/node_modules
    command: npm run dev