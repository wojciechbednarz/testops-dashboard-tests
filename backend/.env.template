# .env.template - DO NOT commit actual passwords
# Copy to .env.local and fill in real values

# ========================================
# AWS Configuration
# ========================================
AWS_REGION=eu-central-1

# ========================================
# RDS Configuration (for recreation script)
# ========================================
DB_INSTANCE_ID=testops-backend-v6-db
DB_NAME=testopsbackendv6db
DB_USERNAME=postgres

# Password - Choose ONE method:
# 1. AWS Secrets Manager (Recommended):
#    aws secretsmanager get-secret-value --secret-id testops-rds-password --region eu-central-1
# 2. Environment variable:
TESTOPS_DB_PASSWORD=<your-password-here>

# Network Configuration
DB_SUBNET_GROUP=testops-backend-v6-db-subnet-group
SECURITY_GROUP_ID=sg-03f93e8e54a6f8239
VPC_ID=vpc-09d5f900e2938b653

# ========================================
# Current RDS Endpoint
# ========================================
# This will change if RDS is recreated!
DB_HOST=testops-backend-v6-db.cf0gqsiu0i1o.eu-central-1.rds.amazonaws.com
DB_PORT=5432

# ========================================
# Elastic Beanstalk Configuration
# ========================================
EB_ENVIRONMENT=testops-backend-v6
EB_APPLICATION=testops-dashboard-tests-backend-aws
EB_CONFIG=saved-config

# ========================================
# Security Notes
# ========================================
# 1. NEVER commit .env.local to git
# 2. Store passwords in AWS Secrets Manager
# 3. Before terminating RDS, create snapshot:
#    aws rds create-db-snapshot \
#      --db-instance-identifier testops-backend-v6-db \
#      --db-snapshot-identifier testops-backup-$(date +%Y%m%d)
