{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p . && cpx \"static/**/*\" dist/static",
    "watch": "tsc -p . --watch"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "typescript": "^5.3.0"
  }
}